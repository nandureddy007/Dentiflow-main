# DentiFlow

## GitHub Pages static demo

The static prototype is available from `docs/` and is intended for GitHub Pages:

- **GitHub Pages:** static demo only
- **Backend:** available only through the Flask deployment
- **Database:** available only through the backend deployment

To enable it, open **Repository -> Settings -> Pages**, choose **Deploy from a branch**, select `main` and `/docs`, then click **Save**.
